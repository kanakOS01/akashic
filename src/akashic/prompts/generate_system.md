### System Document Template

Generate `system/<system-name>.md` for cross-cutting architecture.
Include boundaries, invariants, runtime topology, integration points, configuration, and observability.
